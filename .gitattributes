# Normalize line endings to LF everywhere (matches .editorconfig);
# CI runs on Linux, so no CRLF in the working tree either.
* text=auto eol=lf

# Windows-only scripts keep CRLF
*.bat text eol=crlf

# Binary files
*.png binary
*.jpg binary
*.ico binary
