# Copy to .env and adjust as needed: cp .env.example .env
# All values here match the defaults baked into docker-compose.yml.

POSTGRES_USER=plank
POSTGRES_PASSWORD=plank
POSTGRES_DB=plank
POSTGRES_PORT=5432
API_PORT=8000
